# Level 1 과제 안내

Lv.1의 과제는 세 축입니다. **강의마다 쌓이는 미니 퀴즈**, **모듈별 실습 산출물**, 그리고 **교과목별 모듈 과제**입니다. 앞의 두 축은 매 강의의 결과물이 그대로 과제가 되도록 설계했고, 모듈 과제는 교과목을 마칠 때 성취도 평가 항목을 한 번에 확인하는 용도입니다.

> 교과목·시수·성취도의 기준은 `Physical AI 트랙 한판.xlsx` 의 `성취도` 시트 Lv.1 블록(4개 교과목 · 178h · 성취도 49개)입니다.

---

## 1. 무엇을 제출하나요?

| 구분 | 내용 | 형식 | 제출 시점 |
|------|------|------|-----------|
| **미니 퀴즈** | 각 강의 본문 끝의 5문항 | 텍스트 또는 노트북 마크다운 셀 | 모듈 종료 시 일괄 |
| **실습 산출물** | 강의별 실습 결과 | 강의에 따라 다름(아래) | 모듈 종료 시 일괄 |
| **환경 기록** | `requirements.txt` / `package.xml`·`setup.py` | 텍스트 | 모듈 ①④ / 모듈 ② |
| **모듈 과제** | 교과목별 1개 (성취도 구현 항목당 문제 1개) | 본인 GitHub 저장소 `physicalai-lv1-<이름>` 의 `lv1_module{번호}/` + 태그 `lv1-module{번호}-submit` | 교과목 종료 시 |

### 모듈별 실습 산출물

| 모듈 | 산출물 |
|------|--------|
| ① 컴퓨터/로봇 시스템 | 리눅스·Git 실습 기록, `05` 센서 패키지, `notebooks/06_numpy.ipynb` |
| ② C++/Python/ROS2 | C++ 빌드 결과(`cpp_basics/`), turtlesim 기반 ROS2 패키지(pub/sub·service·action·custom interface), launch 파일과 `params.yaml`, rqt_graph·RViz2 캡처, rosbag 기록, pytest |
| ③ 선형대수 + 3D 좌표 변환 | `notebooks/21~24_*.ipynb` + `src/{vectors,rotation,transform,coordinate_chain}.py` + `tests/` |
| ④ 미니 프로젝트 | `notebooks/25~27_*.ipynb` + `src/pose_pipeline.py` + 발표 자료 |

---

## 2. 배점

### 미니 퀴즈 — 280점

| 모듈 | 강의 수 | 문항 수 | 배점 |
|------|---------|---------|------|
| ① | 7 (OT 포함) | 35 | 70점 |
| ② | 10 | 50 | 100점 |
| ③ | 8 | 40 | 80점 |
| ④ | 3 | 15 | 30점 |
| **합계** | **28** | **140** | **280점** |

문항당 2점입니다. 서술형은 **핵심 키워드가 들어 있으면 정답 처리**하며, "두 가지를 쓰라"는 문항에서 하나만 맞으면 부분 점수 1점입니다.

### 모듈 과제 — 성취도 루브릭 채점

| 교과목 | 과제 | 문제 수 | 성취도 구현 항목 |
|--------|------|---------|------------------|
| ① | 배달 로봇 온보딩 | 3 (+ 실습 3) | 2 (문제 2·3) |
| ② | turtlesim 기반 C++·Python ROS2 패키지 개발 | 10 | 10 |
| ③ | 로봇 좌표 변환 수학 라이브러리 구현 | 6 | 6 |
| ④ | 픽앤플레이스 로봇의 자세 추정과 궤적 생성 | 6 | 6 |
| **합계** | | **25** (+ 실습 3) | **24** |

문제 하나가 **성취도 구현 항목 하나에 1:1로 대응**하며, 항목별 5단계 루브릭(1~5점)으로 채점합니다. 지시문이 요구한 구현을 모두 충족하면 4점, 심화 요건까지 충족하면 5점입니다.

교과목 ① 의 구현 성취도는 **2개**(Ubuntu CLI·SSH·udev / Git·GitHub PR)입니다. 과제 ① 의 문제 2·3 만 루브릭으로 채점하고, 문제 1(연산 분담·실시간성 설계)은 **실습 산출물 점수**로 채점합니다.

### 실습 산출물 — 모듈별 100점

| 항목 | 배점 | 기준 |
|------|------|------|
| 기능 완성도 | 50 | 요구한 동작이 실제로 수행되는가 |
| 검증 | 25 | 노트북 자가 채점 통과 / pytest 통과 |
| 설명 | 15 | **설명** 표시된 마크다운 셀을 채웠는가 |
| 재현성 | 10 | Restart & Run All 통과, 시드 고정, 환경 기록 |

---

## 3. 노트북 실습의 채점 방식

모듈 ③④의 실습 노트북에는 **자가 채점 셀**이 들어 있습니다.

```
[문항 3] 통과
  PASS  p 가 정확
  PASS  r 이 b 와 수직
  PASS  p + r = a
```

- 각 문항의 작성 칸을 채운 뒤 **바로 아래 채점 칸**을 실행하면 통과 여부가 즉시 나옵니다.
- 채점 칸은 수정하지 마세요. 수정된 채점 칸이 발견되면 해당 문항은 0점 처리합니다.
- 마지막 셀의 `summary()` 출력이 **모든 문항 통과**로 나온 상태로 제출합니다.

```
통과 7 / 시도 7 문항
다시 볼 문항: 없음
```

> 자가 채점은 "정답에 도달했는가"만 봅니다. **설명 셀**과 시각화 문항은 사람이 직접 채점하므로, 통과만으로 만점이 되지는 않습니다.

---

## 4. 제출 전 체크리스트

노트북 실습이 있는 모듈(①의 6강, ③, ④)은 다음을 모두 확인하세요.

- [ ] `summary()` 가 모든 문항 통과로 나온다
- [ ] **Kernel → Restart Kernel and Run All Cells** 로 처음부터 끝까지 오류 없이 통과했다
- [ ] 커널이 **Python (pose_lab)** 으로 선택되어 있다
- [ ] 난수를 쓴 곳은 `np.random.default_rng(시드)` 로 고정했다
- [ ] **설명** 이라고 적힌 마크다운 셀을 모두 채웠다
- [ ] 재사용 코드는 `src/` 의 `.py` 모듈로 내보냈고, `!pytest` 가 통과한다
- [ ] `pip freeze > requirements.txt` 로 환경을 갱신했다
- [ ] 미니 퀴즈 답안을 함께 정리했다
- [ ] 제공된 검증 셀·3D 그림 셀을 수정하지 않았다
- [ ] 마지막 커밋에 `lv1-module{번호}-submit` 태그를 달아 push 했고, 저장소 URL 을 제출 폼에 등록했다

> **가장 흔한 감점 사유**는 "내 화면에서는 되던" 노트북입니다. 셀을 위아래로 오가며 실행하면 화면의 코드 순서와 실제 실행 이력이 어긋납니다. **제출 전 Restart & Run All 한 번**이 이 문제의 유일한 해결책입니다(25강 3절).

---

## 5. 자주 묻는 질문

**Q. 자가 채점을 통과하지 못했는데 제출해도 되나요?**
제출은 가능하지만 검증 점수(25점)에서 감점됩니다. 채점 셀의 `FAIL` 줄에 힌트가 함께 나오니 그것을 먼저 읽어 보세요.

**Q. 미니 퀴즈 정답은 어디서 확인하나요?**
정답과 해설은 강사용 자료로 분리되어 있습니다. 채점 후 해설과 함께 돌려드립니다. 먼저 스스로 답을 적어 보는 것이 목적입니다.

**Q. 실습 노트북이 아니라 `.py` 로 제출해도 되나요?**
모듈 ③④는 노트북 제출이 원칙입니다. 설명·코드·그림이 한 파일에 남아야 채점이 가능하기 때문입니다. 다만 재사용 모듈은 반드시 `.py` 로 분리해 함께 제출하세요.

**Q. 노트북에서 `import` 가 실패합니다.**
커널이 가상환경의 파이썬인지 먼저 확인하세요. 첫 셀의 `print(sys.executable)` 출력이 `.venv` 경로를 가리켜야 합니다(25강 2절).
