# Lv.1 과제 발제 문서

> 발제 시 수강생에게 화면으로 보여주며 안내하는 문서입니다. 과제 지시문(Notion 과제 페이지 ①~④)과 함께 사용합니다.
> 배점·채점 체계는 [과제_안내.md](과제_안내.md), 실습 뼈대는 physicalAI 저장소의 `Lv1/` 실습 템플릿 기준입니다.

---

## 1. 발제 개요 — Lv.1 과제는 세 축입니다

| 축 | 무엇인가 | 언제 하나 | 제출 |
|----|----------|-----------|------|
| 미니 퀴즈 | 각 강의 끝 5문항 (총 140문항·280점) | 강의 들은 직후 | 모듈 종료 시 일괄 |
| 실습 산출물 | 강의별 실습 결과물 (모듈별 100점) | 실습 강의 중 | 모듈 종료 시 일괄 |
| **모듈 과제** | 교과목별 1개, 총 4개 (성취도 루브릭 채점) | 교과목 종료 후 | 교과목 종료 시 |

발제 때 강조할 한 문장: **"과제를 위해 새로 공부할 것은 없습니다. 강의에서 한 것을 그대로 문제 순서대로 다시 하면 완성되도록 설계했습니다."**

모듈 과제 4개와 성취도 대응:

| 과제 | 문제 수 | 루브릭 채점 항목 | 예상 소요 |
|------|---------|------------------|-----------|
| ① 배달 로봇 온보딩 | 3 | 2 (문제 2·3) | 6~8시간 |
| ② turtlesim 기반 C++·Python ROS2 패키지 개발 | 10 | 10 (전 문제) | 20~25시간 |
| ③ 로봇 좌표 변환 수학 라이브러리 구현 | 6 | 6 (전 문제) | 12~15시간 |
| ④ 픽앤플레이스 로봇의 자세 추정과 궤적 생성 | 6 | 6 (전 문제) | 12~15시간 |

문제 하나 = 성취도 구현 항목 하나. 5단계 루브릭(1~5점)으로, **지시문의 요구를 모두 충족하면 4점, 심화 요건(관찰·비교·이유 서술)까지 충족하면 5점**입니다. "동작하는 코드"와 "왜 그런지 설명" 둘 다 있어야 만점이라는 점을 발제 때 명확히 안내합니다.

---

## 2. 공통 진행 방식과 제출 규칙

### 진행 방식

1. 과제 지시문의 **문제 순서대로** 진행합니다. 앞 문제의 산출물을 뒤 문제가 재사용하도록 설계되어 있습니다 (②의 인터페이스 패키지, ③의 `src/` 모듈, ④의 모듈 ③ 재사용).
2. 각 문제의 **"구현 내용"이 체크리스트**입니다. 항목 하나가 곧 채점 포인트 하나입니다.
3. 각 문제 끝의 **답안 템플릿**의 빈칸(`___`)을 채우는 것이 보고서 작성의 최소 단위입니다. 템플릿 항목이 비어 있으면 해당 요구사항 미수행으로 간주합니다.
4. 막히면: 강의 교안 해당 절 → 과제 페이지의 힌트 → 질문 채널 순서로 확인합니다. **에러 메시지 전문과 실행한 명령을 함께** 질문에 붙이는 것이 규칙입니다.

### 공통 제출 규칙

- 제출은 **GitHub 저장소**로 합니다 (zip 없음). 본인 계정의 공개 저장소 `physicalai-lv1-<이름>` 하나에 `lv1_module1/` ~ `lv1_module4/` 폴더로 네 과제를 모두 담습니다. 과제 ②③④ 는 이 저장소의 `lv1_module2_student/`·`lv1_module3_student/`·`lv1_module4_student/` 템플릿을 복사해 시작합니다.
- 마감 전 마지막 커밋에 태그 `lv1-module{번호}-submit` 을 달아 push 하고, 저장소 URL 을 제출 폼에 등록합니다. 채점은 그 태그의 커밋으로 하며 이후 커밋은 반영하지 않습니다.
- 터미널 출력은 캡처가 아니라 **텍스트로 복사해 코드 블록으로** 붙입니다. 화면 자체가 결과물인 것(turtlesim 궤적, RViz2, 그래프)만 이미지로 첨부합니다.
- `report.md` 는 문제 번호 순서대로 절을 나눕니다 (`## 문제 1`, `## 문제 2`, …).
- 빌드 산출물(`build/`, `install/`, `log/`, `.venv/`, `__pycache__/`)은 **커밋하지 않습니다** (템플릿의 `.gitignore` 사용).
- 난수를 쓰는 모든 곳은 `np.random.default_rng(42)` 로 시드 고정. **시드가 다르면 수치가 채점 기준과 달라져 오답 처리될 수 있습니다.**
- 노트북 과제(③④)는 제출 전 **Restart Kernel and Run All Cells** 통과가 제출 조건입니다.

### 발제 시 공통 주의사항 멘트

- "지시문에 명시된 이름(토픽·메시지 필드·패키지·파일명)은 채점 스크립트가 그대로 찾습니다. **이름을 바꾸면 동작해도 감점**됩니다."
- "가장 흔한 감점 사유는 '내 화면에서는 되던' 노트북입니다. 셀을 위아래로 오가며 실행했다면 제출 전 Restart & Run All 한 번이 유일한 해결책입니다."
- "실행이 불가능한 환경 항목(예: GUI 없는 서버)은 포기하지 말고, **실행 불가 사유와 대체 확인 방법을 report.md 에 남기면 부분 인정**됩니다."

---

## 3. 과제 ① — 배달 로봇 온보딩 (모듈 ①, 1~4강)

### 발제 멘트

"로봇 회사에 입사한 첫 주를 그대로 해 봅니다. 이 로봇의 어느 계산을 어디서 돌릴지 설계하고, 개발환경을 세우고, 팀 저장소에 첫 PR을 올립니다. **로봇도, 온보드 컴퓨터도, USB 센서도 필요 없습니다** — SSH는 내 리눅스에 서버를 올려 접속하고, udev는 리눅스 기본 loop 장치로 진행합니다."

### 어떤 작업을 하나요?

| 문제 | 어떤 작업을 하나요? | 채점 |
|------|--------------------|------|
| 1. 연산 분담과 실시간성 설계 | 배달 로봇의 여섯 가지 작업(모터 제어~로그 집계)을 임베디드/Edge/클라우드에 배치하고 지연 예산·데이터 전송량으로 근거를 씁니다. 카메라 원시 영상 전송량을 수치로 계산해 클라우드 직송 설계가 성립하지 않음을 보이고, 인지-판단-제어 계층 매핑과 Hard/Firm/Soft 실시간 분류표를 작성합니다. | 실습 산출물 점수 |
| 2. SSH 원격 접속과 udev 장치 고정 | 내 리눅스에 sshd를 올려 키 기반 무비밀번호 접속을 구성하고, 원격 명령 실행과 scp 전송을 수행합니다. loop 장치 2개를 라이다·IMU로 가정해 udev 규칙으로 `/dev/robot_lidar`, `/dev/robot_imu` 고정 이름을 부여하고, 연결 순서를 바꿔도 이름이 유지됨을 검증합니다. | **루브릭** |
| 3. 팀 저장소 협업 | GitHub 저장소를 만들어 브랜치 2개에 문제 1·2의 산출물을 커밋하고, PR을 열어 셀프 리뷰-반영-병합을 수행합니다. 의도적으로 conflict를 만들어 해결하고, merge와 rebase의 이력 그래프를 나란히 비교합니다. | **루브릭** |

### 최종 제출 포맷

```
physicalai-lv1-<이름>/lv1_module1/
├── report.md                      # 문제 1~3의 설계·명령·출력·설명 (절 구분 필수)
├── rules/
│   └── 99-robot-sensor.rules      # 문제 2의 udev 규칙 파일
└── images/                        # 캡처가 필요한 항목만
```

`report.md` 문제 3 절에는 **연습용 저장소 URL과 PR URL**을 반드시 포함합니다. 채점자는 URL로 직접 들어가 PR 리뷰 코멘트·반영 커밋·이력 그래프를 확인하므로, **저장소는 public** 이어야 합니다. 연습용 저장소는 제출 저장소와 별도로 만듭니다.

### 진행·발제 주의사항

- **WSL2는 udev 데몬이 기본으로 꺼져 있습니다.** 네이티브 Ubuntu 또는 가상머신을 권장하고, WSL2를 쓰겠다면 `sudo service udev start` 후 진행하되 문제가 생기면 사유를 보고서에 남기게 안내합니다.
- loop 장치 번호(`/dev/loop10` 등)는 환경마다 다릅니다. 지시문의 예시 번호를 그대로 쓰지 말고 `losetup -f --show` 출력의 실제 번호를 쓰라고 안내합니다.
- 자주 나오는 질문: "SSH를 localhost로 하면 원격이 아니지 않나요?" → 접속 메커니즘(키 교환, pts 세션, `$SSH_CONNECTION`)은 동일하며, 그래서 문제에 **원격 세션 증명 항목**이 들어 있습니다.

---

## 4. 과제 ② — turtlesim 기반 C++·Python ROS2 패키지 개발 (모듈 ②, 7~16강)

### 발제 멘트

"실제 로봇은 수십 개 노드가 메시지를 주고받으며 동작합니다. turtlesim을 로봇 대신 놓고, C++ 빌드 체계부터 pub/sub·Service·Action·커스텀 인터페이스·QoS·launch·검증까지, ROS2 패키지를 밑바닥부터 만듭니다. 문제 10개가 성취도 10개와 1:1입니다. **양이 가장 많은 과제이니 미루지 말고 해당 강의가 끝날 때마다 대응 문제를 푸세요** — 3강이 끝나면 문제 3을 풀 수 있습니다."

### 어떤 작업을 하나요?

| 문제 | 어떤 작업을 하나요? |
|------|--------------------|
| 1. C++ 빌드 체계 | 제동 거리 계산 프로그램을 g++로 다중 파일 수동 빌드(컴파일·링크 분리)하고, `undefined reference`를 의도적으로 재현해 컴파일 에러와 구분합니다. 같은 프로젝트를 CMake로 옮겨 증분 빌드를 관찰합니다. |
| 2. 현대 C++ 센서 계층 | 추상 클래스 `Sensor`와 상속·가상함수·가상 소멸자로 다형성 루프를 만들고, `unique_ptr`(RAII)·STL 컨테이너·함수 템플릿을 사용합니다. 메모리 누수를 재현하고 sanitizer로 검출한 뒤 수정합니다. |
| 3. rclpy 노드 | `/turtle1/pose`를 구독해 원점 거리를 `/turtle_distance`(Float32, 10Hz)로 발행하는 노드, 임계값(기본 2.5) 경고 구독자, 거북이를 정사각형으로 모는 cmd_vel 발행 노드를 작성합니다. 파라미터 2개 선언, Ctrl+C 정상 종료 포함. |
| 4. rclcpp 노드 | 문제 3과 같은 규격의 발행·구독 노드를 C++로 작성하고 colcon 빌드를 통과시킵니다. rclpy 발행→rclcpp 구독 교차 통신을 확인하고 두 언어의 API 대응표를 만듭니다. |
| 5. Service·Action (예제 제공) | turtlesim 내장 서비스 4종을 비동기 호출하고, SetBool/Trigger 자체 서버를 추가합니다. 내장 액션 `rotate_absolute`의 피드백 수신과 취소 처리를 구현하고, 기능별 통신 모델 선택 근거를 표로 정리합니다. |
| 6. 커스텀 인터페이스 (예제 제공) | 전용 패키지에 `Waypoint.msg`(중첩·배열 포함), `SetGain.srv`, `DrawPolygon.action`을 정의·빌드하고, 다각형(삼각형·오각형·팔각형)을 그리는 자체 액션 서버(피드백·취소 포함)를 구현합니다. |
| 7. QoS (예제 제공) | Best-Effort/Reliable 비호환으로 통신 단절을 직접 재현하고 `topic info --verbose`로 진단·복구합니다. Transient Local의 late-join 수신을 확인하고 토픽 5종의 QoS 설계표를 작성합니다. |
| 8. colcon 워크스페이스 (예제 제공) | 패키지 의존성을 `package.xml`에 선언하고 빌드 순서를 확인합니다. `source install/setup.bash` 전후 차이를 환경 변수 관점에서 설명합니다. |
| 9. launch·파라미터 (예제 제공) | turtlesim 포함 4개 노드를 launch 하나로 기동하고, 파라미터를 YAML로 분리해 재빌드 없이 동작을 바꿉니다. 네임스페이스로 두 번째 거북이를 띄웁니다. |
| 10. 시각화·기록·테스트 | rqt_graph 진단 절차, TF 브로드캐스터 작성, RViz2에 TF+마커 표시, rosbag 기록·재생, 순수 계산 함수 3개의 pytest(정상·경계·예외), 예외 처리·logging을 수행합니다. |

### 최종 제출 포맷

```
physicalai-lv1-<이름>/lv1_module2/
├── report.md                  # 문제 1~10 절 구분, 실행 명령·출력·설명
├── cpp_basics/                # 문제 1·2
│   ├── stop_distance.cpp
│   ├── motor.hpp / motor.cpp / main.cpp
│   ├── CMakeLists.txt
│   └── sensors/               # 문제 2 소스
├── ros2_ws/
│   └── src/                   # 문제 3~10 패키지 전체 (build/install/log 제외)
│       ├── turtle_py/         # ament_python — 노드, launch/, config/params.yaml, test/
│       ├── turtle_cpp/        # ament_cmake — C++ 노드
│       └── turtle_interfaces/ # msg/ srv/ action/
├── screenshots/               # turtlesim 궤적·rqt_graph·RViz2
└── bags/                      # 문제 10 rosbag 기록
```

**규격 고정 항목** (바꾸면 감점): 토픽 `/turtle_distance`(Float32·10Hz), 파라미터 `publish_rate`·`warn_distance`, 패키지명 `turtle_py`/`turtle_cpp`/`turtle_interfaces`, 인터페이스명 `Waypoint`/`WaypointList`/`SetGain`/`DrawPolygon`, turtlesim 기본 토픽·서비스 이름.

### 진행·발제 주의사항

- **GUI가 필요합니다.** turtlesim 창이 떠야 하므로 네이티브·가상머신은 그대로, Windows는 WSL2+WSLg, 도커는 X11 포워딩. 발제 때 자기 환경에서 `ros2 run turtlesim turtlesim_node` 가 뜨는지 **이번 주 안에 먼저 확인**하라고 안내합니다.
- 거북이 제어에 필요한 수학은 지시문에 적힌 두 줄(`hypot`, `atan2` + 각도 정규화)이 전부입니다. 경로 계획·장애물 회피는 Lv.2 범위이므로 과하게 구현하지 않아도 됩니다.
- 자주 나오는 질문 예상: "패키지를 하나로 합쳐도 되나요?" → 안 됩니다. 인터페이스 패키지 분리가 문제 6의 채점 포인트(분리 이유 서술)입니다. / "액션 취소가 안 먹혀요" → 콜백 그룹·spin 구조 문제가 대부분, 11강 예제 참조.
- 저장소가 무거워지는 주범은 `build/`·`install/`·bag 파일입니다. 빌드 산출물은 `.gitignore` 로 막고, bag은 두 토픽만 30초 내외로 기록하게 안내합니다.
- 문제 5~9 는 `lv1_module2_student/` 에 예제 코드가 있습니다. "예제를 그대로 내면 되나요?" → 안 됩니다. 예제는 동작 원리를 보여주는 참고용이고, 본인 패키지에 통합해 **실행한 기록(출력·캡처·표)** 을 report.md 에 남겨야 점수가 납니다.

---

## 5. 과제 ③ — 로봇 좌표 변환 수학 라이브러리 구현 (모듈 ③, 17~24강)

### 발제 멘트

"카메라가 본 물체를 로봇 기준 좌표로 바꾸는 일은 현장에서 TF2가 해 주지만, 그 안은 결국 행렬 연산입니다. 이번 과제는 **라이브러리에 기대지 않고** 벡터 연산부터 회전·동차변환·좌표 체인까지 NumPy로 직접 구현합니다. 여기서 만든 `src/` 모듈을 모듈 ④ 미니 프로젝트가 **그대로 import** 하므로, 이 과제가 곧 다음 과제의 준비입니다."

### 어떤 작업을 하나요?

| 문제 | 어떤 작업을 하나요? |
|------|--------------------|
| 1. 벡터 연산 모듈 | 내적·사이각·정규화(영벡터 처리 포함)·정사영(`project`)·반대칭행렬(`skew`)·평면 법선을 구현하고, 각 함수를 수직성·복원성 등으로 검증합니다. 세 벡터의 rank가 3이 아닌 이유를 벡터 관계로 설명하고 `src/vectors.py`로 내보냅니다. |
| 2. 회전 행렬 | `rot_x/y/z`를 직접 작성하고 `rot_z(22.5°)`로 (1,0,0)→(cos22.5°, sin22.5°, 0) 검증, 축별 회전 3D 시각화(그림 코드 제공), **합성 순서에 따른 자세 차이를 나란히 3D 비교**, 행렬식 1과 반사(−1)의 차이 설명, `skew` 기반 로드리게스 공식 구현까지 수행합니다. |
| 3. 재직교화·pytest | 회전을 20만 번 누적해 직교성 붕괴를 수치로 측정하고, 직접 구현한 Gram-Schmidt로 복구해 전후를 비교합니다. 회전행렬 성질 4가지 이상의 pytest를 작성하고, 함수를 일부러 틀리게 바꿔 테스트가 실패를 잡는지 확인합니다. |
| 4. 연립방정식·해의 판정 | 주어진 3원 연립방정식을 가우스 소거로 직접 구현해 단계별 첨가행렬을 출력하고, rank 비교로 해의 유일성을 판정합니다. 피벗팅 유무의 오차 비교, `solve` 대 역행렬 곱셈의 시간·잔차 비교까지 수행합니다. |
| 5. 동차변환·최소자승 | `make_T`/`inv_T`(회전 전치 공식 사용)를 구현·검증하고, 점(w=1)과 방향(w=0)의 변환 차이, 합성 순서 비교, `inv_T` 속도 비교, 과결정 문제의 정규방정식 풀이와 `lstsq` 대조를 수행합니다. |
| 6. 좌표 체인·회전축 복원 | base→link→camera 체인으로 `camera_point_to_base`를 구현하고 왕복 검증, 반복문 없는 점군 일괄 변환, 3D 시각화를 수행합니다. 고유값 분해로 회전축을 복원하고 쿼터니언으로 만들어 SciPy와 비교(부호 차이 설명)한 뒤 `src/coordinate_chain.py`로 내보냅니다. |

### 최종 제출 포맷

```
physicalai-lv1-<이름>/lv1_module3/       # lv1_module3_student/ 템플릿을 복사해 시작
├── notebooks/
│   ├── 01_vectors.ipynb           # 문항마다 설명 셀 → 코드 셀 → 검증 셀(제공) 순서
│   ├── 02_rotation.ipynb
│   ├── 03_reorthogonalize.ipynb
│   ├── 04_linear_system.ipynb
│   ├── 05_transform.ipynb
│   └── 06_chain.ipynb
├── src/
│   ├── vectors.py
│   ├── rotation.py
│   ├── transform.py
│   └── coordinate_chain.py
├── tests/                         # test_rotation.py 포함, pytest 전수 통과
└── requirements.txt
```

제출 조건: 여섯 노트북 모두 **Restart & Run All 통과** + `pytest` 전수 통과 + 시드 42 고정. 검증 셀(`# --- 검증 ---`)과 3D 그림 셀은 **템플릿에 제공**되며 수정하면 해당 문항 0점입니다. 검증 셀이 전부 `[PASS]` 인 상태로 저장합니다.

### 진행·발제 주의사항

- **`np.linalg` 는 검산용으로만** 씁니다. 직접 구현이 요구된 연산(가우스 소거, Gram-Schmidt, `inv_T` 등)을 `np.linalg` 로 대체하면 해당 문항 0점입니다. 검산으로 쓴 곳은 주석으로 명시하게 안내합니다.
- 부동소수점 비교는 `==` 가 아니라 `np.allclose`/`np.isclose`. 발제 때 "0.1+0.2 != 0.3" 예시로 한 번 짚어 주면 질문이 줄어듭니다.
- 자주 나오는 질문 예상: "`skew(a) @ b` 와 `np.cross` 가 미세하게 달라요" → allclose로 비교. / "10만 번 누적해도 오차가 안 커져요" → float32로 캐스팅했는지, 같은 행렬을 재사용하는지 확인.

---

## 6. 과제 ④ — 픽앤플레이스 로봇의 자세 추정과 궤적 생성 (모듈 ④, 25~27강)

### 발제 멘트

"마지막 과제는 미니 프로젝트입니다. 점군에서 물체 자세를 추정하고, 현재 자세에서 그 물체까지 위치는 스플라인으로, 자세는 SLERP로 보간한 궤적을 만들어 **애니메이션으로 시연**합니다. 모듈 ③에서 만든 라이브러리를 그대로 가져와 조립하는 과제라서, ③이 튼튼하면 ④는 빠르게 끝납니다. 결과물에는 발표 자료가 포함됩니다."

### 어떤 작업을 하나요?

| 문제 | 어떤 작업을 하나요? |
|------|--------------------|
| 1. 환경 구성·좌표계 모델링 | venv+ipykernel로 커널을 등록해 인터프리터 경로를 확인하고, 셀 실행 순서를 일부러 뒤섞는 실험으로 노트북의 함정을 체득합니다. base·link·camera·물체 네 좌표계를 정의해 축이 보이는 3D 그림으로 시각화합니다. |
| 2. 변환 파이프라인 통합 | 모듈 ③의 모듈들을 `src/`로 옮기고 `PosePipeline` 클래스로 묶습니다. 점군(N×3)을 반복문 없이 일괄 변환하고 왕복 검증하며, 관절 각도 3종에 따른 base 좌표 변화를 비교합니다. pytest 2개 이상 작성. |
| 3. 쿼터니언·SLERP | 회전행렬↔쿼터니언 양방향 변환과 SLERP를 직접 구현해 SciPy와 대조합니다. 중간 자세 7개 시각화(그림 코드 제공), 단순 선형 보간의 크기 이탈 그래프, 경계 상황(거의 같은/정반대 자세) 처리까지 다룹니다. |
| 4. 궤적 보간·연속성 | 경유점 5~6개를 선형 보간과 큐빅 스플라인으로 잇고 위치·속도·가속도 연속성을 비교합니다. 시작·끝 속도/가속도가 0인 5차 다항식 궤적을 구현·검증하고, 용도별 보간 방식 선택 기준표를 만듭니다. |
| 5. 자세 추정 | 주어진 점군 생성 코드(시드 42, 240점)로 PCA 주축 추출·시각화(그림 코드 제공), 구형 점군에서의 불안정성 관찰, Kabsch로 회전 역추정, 노이즈(0~0.25)×점 개수(30/90/270) 오차 그래프, 이상치 제거 전후 비교를 수행합니다. |
| 6. 시연·발표 | 추정 자세를 목표로 SLERP(자세)+스플라인(위치) 동시 보간 궤적을 만들어 애니메이션으로 재생하고 `demo.gif`로 저장합니다. 궤적 품질을 정량 평가하고, 구조·결과·한계·개선안을 담은 `presentation.md`를 작성합니다. |

### 최종 제출 포맷

```
physicalai-lv1-<이름>/lv1_module4/       # lv1_module4_student/ 템플릿을 복사해 시작
├── notebooks/
│   ├── 01_pipeline.ipynb          # 문제 1·2
│   ├── 02_interpolation.ipynb     # 문제 3·4
│   └── 03_pose_estimation.ipynb   # 문제 5
├── src/
│   ├── pose_pipeline.py           # PosePipeline 클래스
│   └── (모듈 ③에서 가져온 rotation.py, transform.py, coordinate_chain.py)
├── tests/
├── requirements.txt
├── demo.gif                       # 문제 6 시연 애니메이션
└── presentation.md                # 발표 자료 — 구조·결과·영향 분석·한계·개선안 5요소
```

노트북은 문항마다 **설명 셀 → 코드 셀 → 검증·결과 해석 셀** 순서. 검증 셀과 3D 그림·애니메이션 셀은 템플릿에 제공됩니다. 세 노트북 모두 Restart & Run All 통과 + 그래프·출력이 남은 상태로 저장.

### 진행·발제 주의사항

- `demo.gif` 저장에는 Pillow 또는 ffmpeg가 필요합니다. 발제 때 미리 설치를 안내합니다 (`pip install pillow`).
- 점군 생성 코드와 **시드 42는 절대 바꾸지 않습니다.** 오차 수치가 채점 기준과 달라집니다. 노이즈·점 개수를 바꾸는 실험도 문제에 명시된 값으로만.
- 발표(`presentation.md`)는 별도 슬라이드가 아니라 마크다운 문서입니다. 5요소(파이프라인 구조 / 추정 결과와 오차 / 노이즈·점 개수 영향 / 한계 / 개선안)가 각각 절로 드러나야 합니다.
- 자주 나오는 질문 예상: "SLERP에서 정반대 자세일 때 0으로 나눠져요" → 그것이 바로 경계 상황 문항입니다. sin(Ω)≈0 처리 방법을 스스로 정해 서술. / "애니메이션이 노트북에서 재생이 안 돼요" → `%matplotlib widget` 또는 HTML(anim.to_jshtml()) 사용, 저장은 별도로 `save()`.

---

## 7. 채점 안내 (발제 마지막에 공지)

- **루브릭 5단계**: 1점 미제출/무관 → 2점 일부 시도 → 3점 핵심 동작 일부 충족 → **4점 지시문 요구 전부 충족** → **5점 심화 요건(비교 실험·이유 서술·경계 처리)까지 충족**.
- 문제 하나가 성취도 항목 하나입니다. 문제를 건너뛰면 그 성취도는 미달로 기록됩니다.
- 자가 채점·pytest 통과는 "정답 도달"만 봅니다. **설명 셀과 서술 항목은 사람이 채점**하므로 통과만으로 만점이 되지 않습니다.
- 지각·재제출 정책은 운영 공지를 따릅니다.

## 8. 발제 진행 순서 (강사용 메모)

1. 세 축 구조와 배점 (5분) — §1
2. 공통 규칙: 저장소 제출·태그·시드·Restart & Run All·이름 고정 (5분) — §2
3. 과제 ①~④ 순서대로: 발제 멘트 → 제출 포맷 트리 → 주의사항 (과제당 7~10분) — §3~6
4. 채점 방식과 질문 규칙 (5분) — §7, §2-4
5. 질의응답 — 예상 질문은 각 과제의 "진행·발제 주의사항"에 정리되어 있습니다.
